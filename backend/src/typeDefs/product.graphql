type Product {
  id: ID!
  sku: String!
  name: String!
  category: String
  price: Float
  dimensions: String
  totalStock: Int      # Field terkomputasi (Global Stock)
  isLowStock: Boolean
  stocks: [StockItem]
}
