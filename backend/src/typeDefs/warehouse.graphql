type Warehouse {
  id: ID!
  name: String!
  location: String!
  code: String!
  capacity: Int
  stocks: [StockItem]
  staffs: [User]
}

# Relasi Many-to-Many antara Product & Warehouse
type StockItem {
  product: Product!
  warehouse: Warehouse!
  quantity: Int!
}
