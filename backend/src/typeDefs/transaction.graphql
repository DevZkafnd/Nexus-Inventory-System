scalar Date

enum MutationType {
  INBOUND
  OUTBOUND
  TRANSFER
  INITIAL_ADJUSTMENT
}

type StockTransaction {
  id: ID!
  type: MutationType!
  product: Product!
  sourceWarehouse: Warehouse
  targetWarehouse: Warehouse
  quantity: Int!
  timestamp: Date!
  referenceNote: String
}
